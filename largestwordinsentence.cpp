#include<iostream>
using namespace std;

int main(){
    // largest word in a sentence
    int n;
    cin>>n;
    cin.ignore;
    char arr[n+1];
    cin.getline(arr , n);
    cin.ignore;
    int i=0;
    int curlen =0,maxLen=0;
    while(1){
        if()
    }
    return 0;
}